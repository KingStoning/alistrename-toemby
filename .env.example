# ========= AList =========
# AList 地址（建议内网/本机，末尾不要带 / ）
ALIST_URL="http://127.0.0.1:5244"
# AList Token（管理后台 -> 个人设置 -> Token）
ALIST_TOKEN="openlist-xxxx"

# 只填「电视剧/动漫」根目录，多个用逗号分隔。
# ⚠️ 不要把电影根目录放进来，避免误扫电影。
TV_ROOTS="/OneDrive-世纪互联-3/电视剧,/OneDrive-世纪互联-3/动漫"

# ========= TMDB =========
TMDB_KEY="your_tmdb_key"
TMDB_LANG="zh-CN"
# TMDB_API_BASE：直连或代理都支持
# 规则（按你提供的代理约定）：
#   - 官方：https://api.themoviedb.org    -> 程序自动补 /3
#   - 代理：https://tmdb.melonhu.cn      -> 程序自动补 /get
#   - 若你显式写了 /3 或 /get，则保持不变
#
# 代理示例（melonhu）：
#   访问数据： https://tmdb.melonhu.cn/get/search/tv?...  (对应官方 /3/search/tv)
#   访问图片： https://tmdb.melonhu.cn/img/t/p/original/xxx.jpg  (对应 image.tmdb.org)
TMDB_API_BASE="https://tmdb.melonhu.cn"

# ========= 风控/重试（建议先用默认） =========
# AList 读操作（list/search）节流：秒
ALIST_SLEEP_READ="0.6"
# AList 写操作（rename/move/mkdir）节流：秒
ALIST_SLEEP_WRITE="1.0"
# AList 请求失败重试次数（500/502/503/504/429/网络错误）
ALIST_RETRIES="6"
# OneDrive Provider refresh 很容易 500 object not found，默认关闭；需要强刷时再开
ALIST_REFRESH="0"

# 遇到重名冲突怎么处理：suffix（默认）/ skip
ON_CONFLICT="suffix"
# move 时是否逐个移动（更安全，默认 1）
MOVE_INDIVIDUAL="1"

# ========= 命名/规则 =========
# 只要文件名里已有 SxxEyy：永远不改文件名（但可能移动进正确的季文件夹）
# 如果文件名只有 SxxEyy（没有剧名）：默认会补前缀 "剧名 - SxxEyy" 以便刮削
FIX_BARE_SXXEYY="1"
# 季文件夹命名格式
SEASON_FORMAT="S{season:02d}"
# 跳过广告/福利/无关文件夹（正则，可留空使用内置默认）
SKIP_DIR_REGEX=""

# ========= AI（可选，留空则完全不调用 AI） =========
# 你的 AI 网关（兼容 /v1/chat/completions）
AI_BASE_URL="https://co.yes.vg/v1"
AI_API_KEY="sk-your_key_here"
AI_MODEL="gemini-2.0-flash"
AI_SLEEP="1.2"

# ========= 日志网页（可选） =========
# 绑定 0.0.0.0 才能公网访问（记得开安全组/防火墙端口）
LOG_HOST="0.0.0.0"
LOG_PORT="53943"
# 访问时带 ?token=xxx
LOGUI_TOKEN="123456"
# 日志文件路径（可留空）
LOG_FILE=""
# 状态文件：用于断点续跑（可留空，默认 logs/state.jsonl）
STATE_FILE=""
# undo 记录文件（可留空，默认 logs/undo-<time>.jsonl）
UNDO_FILE=""
